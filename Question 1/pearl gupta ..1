#include <iostream>
using namespace std;
int main()
{
    int t;  cin>>t;
    while(t!=0){
        int n;  cin>>n;
        int arr[n]; int i=0;
        for(i=0;i<n;i++){  cin>>arr[i];  }
        int max=arr[0],min=arr[0];
        i=1;
        for(i=1;i<n;i++){
            if(arr[i]>max)
                max=arr[i];  
            if(arr[i]<min)
                min=arr[i];
             
        }
        cout<<max<<" "<<min;
        t--;
    }
    return 0;
}
